const DEFAULTS = {
    APP_NAME: "Level Up Journey",
}

const PATHS = {
    HOME: "/",
    LOGIN: "/auth/login",
    REGISTER: "/auth/register",
    TERMS: "/terms",
    PRIVACY: "/privacy",
}

export default DEFAULTS;
export { PATHS };